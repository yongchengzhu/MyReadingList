<% include ./partials/header.ejs %>

<div class="ui container main">
    <div class="header">Member List:</div> <hr>
    <% users.forEach(function(user) { %>
        <a href="/<%= user.username %>/lists"><%= user.username %></a> <br>
    <% }); %>
</div>

<% include ./partials/footer.ejs %>